{
    "name": "noures/nova-cep",
    "description": "AutoComplete CEP Nova >4.",
    "license": "MIT",
    "keywords": [ "laravel", "nova", "cep"],
    "minimum-stability": "dev",
    "prefer-stable": false,
    "version": "1.0.1",
    "repositories": [
      {
          "type": "composer",
          "url": "https://nova.laravel.com"
      }
  ],
    "require": {
        "php": ">=8.1",
        "illuminate/http": ">=8.0.0",
        "laravel/nova": ">=4.0.0"
    },
    "autoload": {
        "psr-4": {
            "Noures\\NovaCep\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Noures\\NovaCep\\NovaCepServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    }
}
